<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  ROADMAP scenario letter:  d
  Phase 18 SC #2 / Plan 18-03 Task 1.

  Description:
    Same shape as scenario `b` (inbound-rest-dial-answer), but the
    downstream UAS is silent — never answers — for the configured
    no-answer window (>30s, Twilio default; 35s budget here). The
    bridge issues CANCEL toward the dial leg, observes 487 Request
    Terminated, and BYEs the inbound leg with DialCallStatus=no-answer.

  Orchestration:
    run-sipp.sh starts a silent-uas stub (sipp scenario that 100 Trying
    forever, never 180 / 200) on $UAS_PORT. The bridge's `<Dial>`
    handler enforces the no-answer timeout (Plan 16 default 30s + 5s
    grace) and CANCELs.

  Long pause (35s) — this scenario's whole point is exercising the
    no-answer timer. Plan 18-06 operator walk may need to relax this
    if the timer is configured differently in CI.

  Expected sipp exit code: 0 (scenario completed — inbound leg BYEd
    with no-answer reason after the dial leg's CANCEL/487/BYE flow).
-->

<scenario name="inbound-rest-dial-no-answer">
  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:+49401234567@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Phase18-E2E-inbound-rest-dial-no-answer
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-16
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>

  <recv response="200" rtd="true"/>

  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:+49401234567@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- The bridge's outbound INVITE ring timeout fires after
       DIAL_RING_TIMEOUT_S (harness sets this to a short value via
       env override so the scenario completes quickly). Active wait
       budget: 15 s — covers REST POST + ring-timeout + CANCEL/487
       + BYE-relay. -->
  <recv request="BYE" rtd="true" timeout="15000"/>

  <send>
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Content-Length: 0

    ]]>
  </send>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
