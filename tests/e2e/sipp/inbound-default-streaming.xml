<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  ROADMAP scenario letter:  a
  Phase 18 SC #2 / Plan 18-03 Task 1.

  Description:
    UAC sends INVITE to the bridge's SIP listener; expects 100 Trying
    (optional), 180 Ringing (optional), and 200 OK with SDP answer.
    UAC ACKs the 200, holds the dialog briefly (1s pause — long enough
    for the bridge to dial WS, send the Twilio `start` event, and pump
    a few RTP packets), then sends BYE and waits for the 200 response.

  Orchestration:
    No external REST POST is needed for this scenario; the bridge's
    default-streaming behavior is implicit (Phase 12-05 zero-config
    `<Connect><Stream url=$WS_TARGET_URL/>` synthesis). run-sipp.sh
    starts a stub WebSocket target on 127.0.0.1:$WS_PORT before
    launching sipp; the bridge accepts the call only when WS upgrade
    succeeds (otherwise 503 per Phase-15 invariant).

  Expected sipp exit code: 0 (scenario completed; bridge answered 200
    OK with PCMU SDP, accepted ACK, accepted BYE, sent 200 OK back).

  Build-blocking acceptance gate per Plan 18-03 Task 1: this scenario
  MUST exit 0 against a running bridge before Task 1 is considered
  complete.
-->

<scenario name="inbound-default-streaming">
  <!-- INVITE — sipp generates the Call-ID via [call_id]; bridge will
       use it to key the CallSession in callManager. -->
  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Phase18-E2E-inbound-default-streaming
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-16
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>

  <!-- Bridge MUST answer 200 OK with SDP. rtd="true" records
       INVITE-to-200 round-trip-time in --trace_stat output. -->
  <recv response="200" rtd="true">
    <action>
      <!-- Verify the SDP answer carries PCMU/8000 (codec discipline).
           sipp's <ereg> couples check_it with assign_to; we read $codec
           in a follow-up <log> so sipp 3.6+ doesn't flag it as
           "Variable referenced 1 times". -->
      <ereg regexp="a=rtpmap:0 PCMU/8000" search_in="body" check_it="true" assign_to="codec"/>
      <log message="codec match: [$codec]"/>
    </action>
  </recv>

  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Phase18-E2E-inbound-default-streaming
      Content-Length: 0

    ]]>
  </send>

  <!-- 1 second hold — the bridge has time to upgrade WS, emit
       `connected`/`start` events, and pump a few RTP frames before
       BYE arrives. The actual RTP exchange is not orchestrated here
       (run-sipp.sh defaults are sufficient for the canonical flow). -->
  <pause milliseconds="1000"/>

  <send retrans="500">
    <![CDATA[

      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Phase18-E2E-inbound-default-streaming
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true"/>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
