<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  ROADMAP scenario letter:  h
  Phase 18 SC #2 / Plan 18-03 Task 1.

  Description:
    UAC sends INVITE → 200 OK → ACK → short streaming → BYE → 200.
    The bridge's status callback is configured (via per-call REST set
    OR env-var-default `STATUS_CALLBACK` URL) to a flapping HTTP host
    that returns 502 on the first request and 200 on the retry. The
    bridge's exp-backoff retry policy (3× per Phase 16) eventually
    succeeds. The orchestrator (run-sipp.sh) verifies via the flapping
    server's request log that the bridge re-tried at least once.

  Orchestration:
    run-sipp.sh starts a Python flapping-http stub on $FLAPPING_PORT
    that alternates 502 / 200 responses. Before launching sipp, the
    orchestrator either:
      (a) sets the bridge's STATUS_CALLBACK env to point at the stub
          and accepts the bridge's default callback emission, OR
      (b) fires a REST POST setting the per-call statusCallback URL
          and event vocab.
    Approach (a) is simpler and what run-sipp.sh implements by default.

  This scenario itself drives only the SIP side; the orchestrator
  verifies the callback retry behavior outside sipp.

  Expected sipp exit code: 0 (SIP flow completes).
  Additional run-sipp.sh assertion: flapping host saw ≥2 POST attempts
  per call (verified against /tmp/flapping-server.log).
-->

<scenario name="status-callback-flapping-host">
  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:+49401234567@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Phase18-E2E-status-callback-flapping-host
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-16
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>

  <recv response="200" rtd="true"/>

  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:+49401234567@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <pause milliseconds="2000"/>

  <send retrans="500">
    <![CDATA[

      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:+49401234567@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true"/>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
