<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  ROADMAP scenario letter:  g
  Phase 18 SC #2 / Plan 18-03 Task 1.

  Description:
    UAC sends INVITE → 200 OK auto-answer (default streaming) → ACK →
    short pause for streaming → orchestrator fires REST POST with
    `Twiml=<Response><Hangup/></Response>` → bridge sends BYE on the
    inbound leg → UAC responds 200.

  Orchestration:
    run-sipp.sh fires
      curl -X POST -u $ACCOUNT_SID:$AUTH_TOKEN \
        -d "Twiml=<Response><Hangup/></Response>" \
        http://localhost:$HTTP_PORT/2010-04-01/Accounts/$ACCOUNT_SID/Calls/$CALL_SID.json
    1.5 seconds after the inbound 200 OK, mid-stream.

  Expected sipp exit code: 0.
-->

<scenario name="rest-hangup-mid-stream">
  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:+49401234567@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Phase18-E2E-rest-hangup-mid-stream
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-16
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>

  <recv response="200" rtd="true"/>

  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:+49401234567@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- Wait actively for BYE — the orchestrator fires the REST POST during
       this window, the bridge then sends BYE on the inbound leg. Using
       <recv> with timeout (vs <pause> + <recv>) so an early BYE is not
       treated as "unexpected". 5s budget covers orchestrator latency. -->
  <recv request="BYE" rtd="true" timeout="5000"/>

  <send>
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Content-Length: 0

    ]]>
  </send>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
