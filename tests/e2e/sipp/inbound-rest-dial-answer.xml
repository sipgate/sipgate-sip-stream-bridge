<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  ROADMAP scenario letter:  b
  Phase 18 SC #2 / Plan 18-03 Task 1.

  Description:
    UAC sends INVITE → 200 OK → ACK → pause (orchestrator window) →
    expects BYE from the bridge once the dial leg completes → sends
    200 to the bridge's BYE.

  Orchestration:
    During the post-ACK pause, run-sipp.sh fires
      curl -X POST -u $ACCOUNT_SID:$AUTH_TOKEN \
        -d "Twiml=<Response><Dial><Number>+49301234567</Number></Dial></Response>" \
        http://localhost:$HTTP_PORT/2010-04-01/Accounts/$ACCOUNT_SID/Calls/$CALL_SID.json
    The downstream targeted by `<Dial>` is a UAS stub (run-sipp.sh
    starts a sipp -sn uas instance on port $UAS_PORT) that answers
    200 OK. The bridge tears down the WS stream (privacy gate), B2BUAs
    the call, and on dial-leg BYE relays BYE to the inbound leg. This
    scenario observes the inbound leg only — sees the BYE arrive.

  Caller-ID:
    The From URI uses sip:+49401234567@[local_ip] (a recognizable
    sipgate-shaped E.164 address) so the orchestrator can verify ANI
    preservation when the bridge issues the outbound INVITE.

  Expected sipp exit code: 0 (scenario completed — bridge BYEd the
    inbound leg cleanly after the dial leg answered).
-->

<scenario name="inbound-rest-dial-answer">
  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:+49401234567@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Phase18-E2E-inbound-rest-dial-answer
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-16
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>

  <recv response="200" rtd="true"/>

  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:+49401234567@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- Wait actively for BYE — the orchestrator fires the REST POST
       during this window, the dial-leg UAS answers + BYEs after a short
       pause, the bridge relays the BYE to the inbound leg. 10s budget
       covers orchestrator latency + UAS answer + UAS hangup pause. -->
  <recv request="BYE" rtd="true" timeout="10000"/>

  <send>
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Content-Length: 0

    ]]>
  </send>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
