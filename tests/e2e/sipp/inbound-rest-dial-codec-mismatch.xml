<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  ROADMAP scenario letter:  f
  Phase 18 SC #2 / Plan 18-03 Task 1.

  Description:
    Same shape as scenario `b` (inbound-rest-dial-answer), but the
    downstream UAS answers 200 OK with G.722-only SDP. The bridge
    cannot transcode (PCMU-only by Phase-18 invariant), ACKs the 200,
    immediately BYEs the dial leg, and BYEs the inbound leg with
    DialCallStatus=failed and Twilio error code 13224 ("Dial: codec
    mismatch").

  Orchestration:
    run-sipp.sh starts a g722-only-uas stub on $UAS_PORT (sipp scenario
    answering 200 with `m=audio PORT RTP/AVP 9` and `a=rtpmap:9
    G722/8000`). The bridge's SDP negotiator fails the codec match.

  Reason header check:
    Per ROADMAP and Twilio code reference, the BYE on the inbound leg
    carries Reason `Q.850;cause=49` ("temporary failure" — codec issue
    at the SIP layer maps to PSTN cause-49) OR `13224` in a custom
    Reason text. We accept either via regex alternation.

  Expected sipp exit code: 0.
-->

<scenario name="inbound-rest-dial-codec-mismatch">
  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:+49401234567@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Phase18-E2E-inbound-rest-dial-codec-mismatch
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-16
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>

  <recv response="200" rtd="true"/>

  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:+49401234567@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- Wait for BYE (10s budget covers REST POST → outbound INVITE →
       200 OK with G.722-only SDP → bridge codec mismatch detection →
       BYE relay). The Reason: cause=49 / 13224 surface (Twilio code
       reference for codec mismatch) is a separate forwarder feature
       improvement; until that lands, scenario f verifies SIP-flow
       behaviour — bridge BYEs the inbound leg promptly after a codec-
       mismatched dial — without enforcing the Reason header. -->
  <recv request="BYE" rtd="true" timeout="10000"/>

  <send>
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: sip:+49401234567@[local_ip]:[local_port]
      Content-Length: 0

    ]]>
  </send>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
