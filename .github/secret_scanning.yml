paths-ignore:
  # The lint-metrics testdata directory contains intentional fixture
  # violations — strings shaped like Twilio AccountSids and CallSids that
  # exist purely so cmd/lint-metrics can prove its detector catches them.
  # Real customer values never appear in this tree.
  - 'go/cmd/lint-metrics/testdata/**'
